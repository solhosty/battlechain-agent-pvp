{
  "name": "battlechain-agent-pvp",
  "version": "1.0.0",
  "description": "BattleChain PvP Agent Arena - AI agents competing to exploit vulnerable contracts",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "setup": "forge install && npm install && cd frontend && npm install",
    "setup:foundry": "curl -L https://foundry.paradigm.xyz | bash && foundryup",
    "build:contracts": "forge build && node scripts/copy-abis.js",
    "build:frontend": "cd frontend && npm run build",
    "dev": "concurrently \"npm run dev:contracts\" \"npm run dev:frontend\"",
    "start:local": "anvil --fork-url $BATTLECHAIN_RPC_URL",
    "dev:contracts": "anvil --fork-url $BATTLECHAIN_RPC_URL 2>/dev/null || echo 'Install Anvil: curl -L https://foundry.paradigm.xyz | bash'",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "forge build && cd frontend && npm run build",
    "test": "forge test -vvv",
    "test:gas": "forge test --gas-report",
    "deploy": "forge script script/Deploy.s.sol --rpc-url $BATTLECHAIN_RPC_URL --broadcast --verify",
    "deploy:battlechain": "forge script script/Deploy.s.sol --rpc-url $BATTLECHAIN_RPC_URL --broadcast --verify",
    "deploy:battlechain:estimate": "forge script script/Deploy.s.sol --rpc-url $BATTLECHAIN_RPC_URL --sig \"run()\" --estimate-gas",
    "verify:battlechain": "forge verify-contract $CHALLENGE_FACTORY_ADDRESS src/ChallengeFactory.sol:ChallengeFactory --rpc-url $BATTLECHAIN_RPC_URL --verifier-url $BATTLECHAIN_API_URL --etherscan-api-key $ETHERSCAN_API_KEY && forge verify-contract $ARENA_ADDRESS src/Arena.sol:Arena --constructor-args $(cast abi-encode \"constructor(address,address,address)\" $ATTACK_REGISTRY_ADDRESS $SAFE_HARBOR_ADDRESS $CHALLENGE_FACTORY_ADDRESS) --rpc-url $BATTLECHAIN_RPC_URL --verifier-url $BATTLECHAIN_API_URL --etherscan-api-key $ETHERSCAN_API_KEY && forge verify-contract $VITE_BETTING_ADDRESS src/SpectatorBetting.sol:SpectatorBetting --constructor-args $(cast abi-encode \"constructor(address)\" $ARENA_ADDRESS) --rpc-url $BATTLECHAIN_RPC_URL --verifier-url $BATTLECHAIN_API_URL --etherscan-api-key $ETHERSCAN_API_KEY",
    "deploy:local": "forge script script/Deploy.s.sol --rpc-url http://localhost:8545 --broadcast",
    "lint": "forge fmt --check && cd frontend && npm run lint",
    "lint:fix": "forge fmt && cd frontend && npm run format",
    "clean": "forge clean && cd frontend && rm -rf dist node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
